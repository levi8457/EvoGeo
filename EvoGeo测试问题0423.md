# http://localhost:5174/perception/dashboard

* 首次打开页面时会去调用一次接口，虽然页面没有展示信息，但是接口返回了相关数据

* 未选择品牌时点击查询也返回了数据，请求头中并没有对应的品牌查询查询参数
* 选择平台后点击查询，返回的数据内容无变化，页面展示也对应无变化
* 刷新竞品对比雷达图时，调用的接口/api/perception/competitor-analysis返回404

# http://localhost:5174/perception/queries

* 单条数据的详情页面
  1. 点击立即检测后是否会按平台筛选展示数据，如需要，目前未实现此筛选效果，仍为全量展示
     2. 未对查询结果进行分页处理
* 所有检测按钮均会提示对应平台API未配置
* 

# http://localhost:5174/compliance/management

* 点击单条数据的查看按钮有调用接口，但页面上没有相应的变化

# http://localhost:5174/evolution/dashboard

* 无按品牌筛选的按钮，未展示当前数据对应的品牌
* 分页功能实际无效，页面上依旧展示所有数据

# http://localhost:5174/evolution/strategies

* 无按品牌筛选框
* 策略详情的编辑页面，状态的对应选项要不要从英文改为中文？修改策略类型失败，前端的请求并未包涵对应变更参数。每次修改会导致策略参数中的json字段内累计增加反斜杠字符
* 添加策略的字段未作必填校验，可直接保存为空
* 对某条时间优化类数据进行进化操作时，变异率和交叉率各自拉高后，点击进化，会将原内容模板一栏的内容完全替换为“增强”或“调整”两个字来生成一条新的策略数据，代数字段值+1，无其它变化，不了解这块儿的业务需求逻辑，此条仅做记录（其余策略类型的数据进行相同操作的区别为替换的文字不同，其它表现一致）
* 添加评估时调用http://localhost:5174/evolution/strategies POST接口，提示404，页面提交时也未作必填校验

# http://localhost:5174/generation/contents

* 生成内容时选择策略的下拉框展示的字段为strategyType策略类型而非contentTemplate模板内容
* /api/generation/contents GET 内容列表的搜索框未做英文的大小写匹配处理，ai不能搜索出AI，是否需要处理？
* 未找到“已归档”相关操作入口，此项未进行测试

# http://localhost:5174/memory/management

* 记忆数据的类型一览展示的是英文，是否改为对应中文字段？
* 点击一条记忆数据的查看按钮时，会使访问次数的值加一，是否合乎设计
* 添加记忆时，记忆键字段目前允许和已有数据相同，是否需要做唯一性处理
* /api/memory/entries GET 搜索记忆键的搜索框未做英文的大小写匹配处理，ai不能搜索出AI，是否需要处理？
* /api/memory/entries/ PUT  编辑记忆数据时，接口只给后端传了记忆内容和重要性两个字段导致其余字段的修改均无效

# http://localhost:5174/execution/dashboard

* 未找到添加数据入口， 未测试

# http://localhost:5174/compliance/management

* 选择待检测内容的查看按钮时，页面展示内容无任何变化，点击合规检测后，查看检测结果为通过，但页面上对应数据的合规状态仍为未检测

未找到可以修改品牌相关信息的入口（如新增、修改、删除）